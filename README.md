Wavr — Frontend scaffold

Run:

1. copy `.env.example` to `.env` and set `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`
2. npm install
3. npm run dev

This repository contains a minimal implementation of the Wavr front-end (React + Vite + TypeScript).
